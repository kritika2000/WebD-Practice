// JS comments are simply nites for other developers.
// Should use camelCase for declaring variables.
let myVariable;
typeof myVariable; // undefined if no value is assigned during declaration.
// reassigning the variable.
myVariable = 20;
// + operator concatenates data when used with with string data type -> 88 + "hello" = "88hello"
